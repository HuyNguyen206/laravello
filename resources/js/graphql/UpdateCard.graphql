mutation($id: ID!,$title: String){
    updateCard(id: $id, title: $title){
        id
        title
        order
        owner{
            id
            name
        }
        cardList{
            id
            board{
                id
            }
        }
    }
}
