query($userId: ID!){
    userBoards(user_id: $userId){
        id
        title
        color
        cardLists
        {
            id
            title
        }
        owner{
            id
            name
            email
        }
    }
}
